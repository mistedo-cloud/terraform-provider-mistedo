# DNS — MX with optional `priority`

Shows **`mistedo_dns_record`** with an **optional** field that matters for **MX**: `priority`.

Use a **zone** you are allowed to create. Set `mail_host` to a real hostname if you send real mail.
