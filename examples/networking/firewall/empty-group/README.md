# Compute — security group, no rules yet

**Required-only** **`mistedo_security_group`**: **`name`**.

Use this when you want an **empty** group and will attach rules in a separate configuration or later apply.
