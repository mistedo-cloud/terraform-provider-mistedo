# Compute — network + DNS resolvers (optional field)

Extends the minimal network example with **`dns_nameservers`**. Once set, Terraform **refreshes** the list from the API on read.

Adjust **`dns_nameservers`** to resolvers your platform accepts.
